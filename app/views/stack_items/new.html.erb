<% provide(:title, 'New Submission Detail') %>

<%= form_for([@sub, @comment]) do |f| %>
<%= f.label :comment %>
<%= f.text_area :comment %>

<%= f.hidden_field :sub_id, :value => @sub.id %>


<%= f.submit "Add" %>
<% end %>
